# clg-1st-year-prject
   sudo apt update -y
   sudo apt install -y git
  cd ~
   git clone https://github.com/rajkumardusad/Tool-X.git
   cd Tool-X
   chmod +x install.aex
   ./install.aex
      Tool-X
